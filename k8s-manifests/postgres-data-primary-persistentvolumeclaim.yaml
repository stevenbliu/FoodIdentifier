apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: postgres-data-primary
  name: postgres-data-primary
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
