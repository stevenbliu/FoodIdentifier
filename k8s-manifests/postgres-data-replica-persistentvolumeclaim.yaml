apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: postgres-data-replica
  name: postgres-data-replica
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
