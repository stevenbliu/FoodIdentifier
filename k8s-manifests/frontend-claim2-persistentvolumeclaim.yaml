apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: frontend-claim2
  name: frontend-claim2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
